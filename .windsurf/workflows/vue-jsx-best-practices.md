---
description: Vue JSX and TSX syntax patterns and differences from React JSX
---

# Vue JSX Best Practices

Use this workflow for Vue JSX or TSX tasks, render functions using JSX, JSX plugin configuration, and JSX syntax differences from React.

1. Before coding, read and follow:

   `d:\AI\TWX_PSE\TW_EngineeringRelease\.trae\skills\vue-jsx-best-practices\SKILL.md`

2. If the skill points to files under `reference/`, read the relevant reference before implementation.

3. Pay special attention to:
   - Vue JSX syntax differences from React JSX.
   - Using `class` rather than React-style `className` when appropriate for Vue JSX.
   - Event and slot syntax.
   - TypeScript typing for props and emits.

4. Prefer SFC templates unless the requirement specifically needs JSX/TSX.
